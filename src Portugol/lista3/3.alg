inicio
    inteiro ano,mes,dia,at,mt,dt,total
    ler ano,mes,dia
        at <- ano * 365
        mt <- mes * 30
        dt <- dt + dia
            total <- at + mt + dt
            escrever "\no total de dias é: ",total
fim

            
        